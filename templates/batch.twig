{% set items = ['a', 'b', 'c', 'd', 'e', 'f', 'g'] %}

<table border="1">
{% for row in items|batch(3, 'No item') %}
    <tr>
        {% for column in row %}
                <td width="100">{{ column }}</td>
        {% endfor %}
    </tr>
{% endfor %}
</table>
